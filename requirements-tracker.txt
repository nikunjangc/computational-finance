# Core (required) — the offline demo and the engine run on just these.
PyYAML>=6.0
requests>=2.28

# Live connectors (optional) — install the ones you actually use.
youtube-transcript-api>=0.6     # YouTube live-caption / transcript source
websockets>=11.0                # generic "open socket" push feed source
redis>=5.0                      # ONLY for automatic multi-instance failover (REDIS_URL)
anthropic>=0.40                 # ONLY for the optional budget-capped LLM classifier (LLM_ENABLED)
Pillow>=10.0                    # ONLY for the branded image card on social posts (POST_IMAGE=true)

# Notes:
#  * Telegram & WhatsApp notifiers use `requests` only (no SDK needed).
#  * Twilio WhatsApp works with plain HTTP; the `twilio` SDK is NOT required.
